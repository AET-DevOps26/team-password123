| Person | Date Range | Tasks Completed |
|---|---|---|
| Melisa | 18/05/26 - 22/05/26 | <ul><li>Implemented the genai-service as a Python FastAPI application supporting multi-provider vision analysis (Ollama locally, OpenAI GPT-4o Vision, Google Gemini) via LangChain, with a configurable provider at startup.</li><li>Built the nutrition data pipeline: imported USDA nutritional data, cleaned and normalized it into a local nutrition_db.json cache, and wrote utility scripts for bulk analysis and result verification.</li><li>Implemented image analysis endpoints in FastAPI: /analyze accepts a base64 image, calls the configured LLM, and returns structured JSON with identified foods, estimated quantities, and macro breakdowns.</li><li>Wrote initial pytest test suite covering smoke tests, nutrition data lookup, and image analysis expectations to validate response structure.</li></ul> |
