| Person | Date Range | Tasks Completed |
|---|---|---|
| Melisa | 01/06/26 - 05/06/26 | <ul><li>Added GitHub Actions CI (ci.yml) as the project's first automated pipeline, covering JUnit tests for all three Java services in a matrix build and Vitest unit tests for the React frontend.</li><li>Wrote the first unit test suites for all backend services: AuthServiceTest, UserServiceTest, JwtServiceTest (auth-service); MealServiceTest, MealMapperTest, GenAiMealAnalyzerTest (meals-service); AnalyticsServiceTest, GoalServiceTest (analytics-service).</li><li>Implemented missing backend endpoints that were needed for the full user workflow: user profile update, photo meal analysis via the GenAI service, and daily logging streak calculation.</li><li>Added the web frontend container to docker-compose.yml so the full stack (all four backend services + React dev server) can be started with a single docker compose up command.</li><li>Fixed a bug in the analytics and diary date handling that caused incorrect day offsets when the frontend consumed the API response.</li><li>Added a PowerShell demo data seeder script (seed-demo-data.ps1) to pre-populate a local dev environment with realistic meal and analytics data.</li></ul> |
