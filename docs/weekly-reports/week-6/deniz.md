| Person | Date Range | Tasks Completed |
|---|---|---|
| Deniz | 08/06/26 - 12/06/26 | <ul><li>Built the full Helm chart for Kubernetes deployment (helm/calorieasy), covering all five services (auth, meals, analytics, genai, web), PostgreSQL, in-namespace Traefik load balancer, NGINX ingress, and a TLS secret.</li><li>Wired GHCR pull secret into the Helm chart and wrote a comprehensive chart README covering deployment steps, access patterns, and environment variable reference.</li><li>Added Phase 2 IaC: Terraform configuration for Azure VM provisioning (azurerm, Ubuntu 22.04) and an Ansible playbook for idempotent Docker Compose deployment to the VM.</li><li>Added two new GitHub Actions workflows: deploy-aet.yml (Helm deploy to AET Kubernetes cluster triggered after image build) and deploy-azure.yml (Terraform validate + Ansible, manual-only).</li><li>Wired the full photo scan flow from the React frontend through the meals-service GenAiMealAnalyzer to the genai-service vision endpoint, making photo-based meal logging end-to-end functional.</li><li>Fixed CI deployment ordering so that the Helm deploy is triggered only after image build succeeds, and switched from latest tags to immutable commit SHA image references to ensure reliable rollbacks.</li><li>Enabled TLS on the K8s ingress via cert-manager with letsencrypt-prod, and set the AET ingress host. Defaulted genai provider to Gemini for the cloud environment.</li></ul> |
