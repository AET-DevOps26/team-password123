| Person | Date Range | Tasks Completed |
|---|---|---|
| Deniz | 22/06/26 - 26/06/26 | <ul><li>Added Prometheus metrics endpoint to the Spring Boot services and set up a basic Grafana dashboard for HTTP request rates and JVM memory.</li><li>Continued monitoring setup and reviewed the overall system state ahead of the final presentation.</li><li>Fixed photo meal logging on the web app: scan fallback now uploads the captured photo (setPhotoLog was never called), added an optional photo dropzone to manual entry (uploadPhoto + convert-manual), and synced PHOTO_AI/ANALYZED meal status enums (todo #2).</li><li>Home and profile polish: time-of-day greeting (morning/afternoon/evening) and a single display-name source shared between Home and Sidebar, re-synced from the authenticated account on login (todo #4, #5).</li><li>Accessibility fixes: distinct aria-labels on auth mode tabs (resolves Sign-in tab vs submit collision), Increase/Decrease labels on onboarding steppers, named scan buttons, and a labelled tab-bar FAB (todo #6, #7, #8).</li><li>GenAI local dev wiring: added APP_GENAI_BASE_URL to docker-compose so photo scan hits genai-service locally, plus .env.example for OpenAI-compatible provider overrides (todo #11).</li><li>Repo cleanup: untracked ios-app/build artifacts, .DS_Store, .idea, and genai-service/.env; added ios-app/.gitignore; ESLint + Prettier config and a CI lint step; updated e2e specs for the greeting and stepper-label changes (todo #10).</li></ul> |
