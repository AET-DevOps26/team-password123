| Person | Date Range | Tasks Completed |
|---|---|---|
| Melisa | 22/06/26 - 26/06/26 | <ul><li>Added the genai pytest job to the CI pipeline so all three test suites run automatically on each push.</li><li>Updated service READMEs and cleaned up outdated references in the root README.</li><li>Built the meal detail flow on the web app: tap a diary entry to view full nutrition, edit items, or delete the meal (new MealDetailModal + useMealDetail hook).</li><li>Implemented AI food estimation for manual entry when a food is not in the quick-match list — full stack from genai-service POST /api/estimate through meals-service to the frontend AiEstimateCard with a live portion picker (grams +/-, suggested serving size, per-100g macros scaled on save).</li><li>Expanded nutrition_db.json from ~990 to 1,381 entries with clean simple keys (breakfast staples, German/Bavarian dishes, seafood, international meals, sauces, snacks, drinks) so common foods like toast resolve instantly without an LLM call.</li><li>Tested and configured local AI providers (Ollama/moondream for photo scan, Logos for text estimation); wired TEXT_OPENAI_* in docker-compose for a dedicated text LLM separate from the vision model.</li><li>Fixed production deployment wiring: restored Gemini as the primary vision LLM in Helm/CI and routed Logos only to text estimation (the deploy workflow had been sending photo scan to Logos, which is text-only).</li></ul> |
