| Person | Date Range | Tasks Completed |
|---|---|---|
| Pavel | 22/06/26 - 26/06/26 | <ul><li>Refactored the frontend to always run against the real backend: removed all frontend mocks (meal, nutrition, auth fallbacks, mock flags) so every page hits live API endpoints.</li><li>Added a DB seed script (scripts/seed.mjs + make seed) that pre-populates one demo user and ~10 days of meals/water — enables a one-command local start without AI/Ollama (compose up non-AI services, then node scripts/seed.mjs, log in with dev@local.com / password123).</li><li>Documented the no-AI local workflow in the root README: full end-to-end flow works (profile, goals, diary, insights, streak); scan button falls back to manual entry when GenAI is not running.</li><li>Manually run through the full user flow on an M1 Mac and verified the seeded setup runs smoothly in ~1 minute after compose build (todo #9).</li><li>Connected user-uploaded photos in the diary so meal entries show the correct photo from the backend (PR #71).</li><li>Removed the login-page preview/dev shortcut and cleaned up seed configs after review feedback.</li><li>Fixed minor UI bugs found during e2e test runs and polished the photo scan result display.</li></ul> |
