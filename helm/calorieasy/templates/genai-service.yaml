{{- if .Values.genai.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: genai-service
  labels:
    app: genai-service
    {{- include "calorieasy.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: genai-service
  template:
    metadata:
      labels:
        app: genai-service
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: genai-service
          image: {{ include "calorieasy.image" (dict "root" $ "name" "genai-service") }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8084
          env:
            - name: PORT
              value: "8084"
            - name: LLM_PROVIDER
              value: {{ .Values.genai.llmProvider | quote }}
            - name: OPENAI_BASE_URL
              value: {{ .Values.genai.openaiBaseUrl | quote }}
            - name: OPENAI_MODEL
              value: {{ .Values.genai.openaiModel | quote }}
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "calorieasy.secretName" . }}
                  key: OPENAI_API_KEY
            - name: BACKUP_OPENAI_BASE_URL
              value: {{ .Values.genai.backupOpenaiBaseUrl | quote }}
            - name: BACKUP_OPENAI_MODEL
              value: {{ .Values.genai.backupOpenaiModel | quote }}
            - name: BACKUP_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "calorieasy.secretName" . }}
                  key: BACKUP_OPENAI_API_KEY
            - name: TEXT_OPENAI_BASE_URL
              value: {{ .Values.genai.textOpenaiBaseUrl | quote }}
            - name: TEXT_OPENAI_MODEL
              value: {{ .Values.genai.textOpenaiModel | quote }}
            - name: TEXT_OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "calorieasy.secretName" . }}
                  key: TEXT_OPENAI_API_KEY
            - name: NUTRITION_DATA_PROVIDER
              value: {{ .Values.genai.nutritionDataProvider | quote }}
            - name: USDA_FDC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "calorieasy.secretName" . }}
                  key: USDA_FDC_API_KEY
            # RAG health-insights: vector store + embedding provider.
            - name: WEAVIATE_URL
              value: {{ .Values.weaviate.url | quote }}
            - name: EMBED_PROVIDER
              value: {{ .Values.genai.embedProvider | quote }}
            {{- if .Values.tracing.enabled }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ .Values.tracing.otelExporterEndpoint | quote }}
            - name: OTEL_SERVICE_NAME
              value: genai-service
            {{- end }}
          startupProbe:
            tcpSocket: { port: 8084 }
            failureThreshold: 30
            periodSeconds: 5
          readinessProbe:
            tcpSocket: { port: 8084 }
            periodSeconds: 10
          livenessProbe:
            tcpSocket: { port: 8084 }
            periodSeconds: 15
          resources:
            {{- toYaml .Values.genai.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  labels:
    app: genai-service
    {{- include "calorieasy.labels" . | nindent 4 }}
spec:
  selector:
    app: genai-service
  ports:
    - port: 8084
      targetPort: 8084
{{- end }}
