# Copy to inventory.ini and fill in the VM public IP from `terraform output public_ip`.
[azure]
YOUR_VM_PUBLIC_IP

[azure:vars]
ansible_user=azureuser
ansible_ssh_private_key_file=~/.ssh/calorieasy_id
