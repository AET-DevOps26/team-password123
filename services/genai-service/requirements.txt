# FastAPI and async server
fastapi==0.104.1
uvicorn==0.24.0
websockets>=12.0,<14
numpy==2.4.4

# LangChain and LLM integrations
langchain>=0.2.0,<1.0
langchain-core>=0.2.0,<1.0
langchain-openai>=0.2.0,<1.0
langchain-google-genai>=2.0.0,<3.0.0

# RAG health insights: Weaviate vector store + local ONNX embeddings.
# fastembed gives BAAI/bge-small-en-v1.5 (384-dim) with no torch dependency,
# keeping the image light; the Weaviate v4 client talks to the vector DB.
weaviate-client>=4.9,<5
fastembed>=0.4,<1

# Image processing
pillow==10.4.0

# HTTP requests
requests==2.31.0

# httpx is a transitive dep (openai, weaviate-client). Pin it into the window
# every consumer allows AND that Starlette 0.27's TestClient supports: httpx 0.28
# dropped the `app=` shortcut that fastapi 0.104's TestClient relies on, so the
# RAG insight tests need httpx<0.28 (weaviate needs >=0.26).
httpx>=0.27,<0.28

# Prometheus metrics (6.1.0 is the newest that keeps starlette<0.28, matching the
# pinned fastapi 0.104.1; 7.x requires starlette>=0.30 and breaks the image build)
prometheus-fastapi-instrumentator==6.1.0

# Distributed tracing: OpenTelemetry FastAPI instrumentation exporting spans over
# OTLP/HTTP. Only active when OTEL_EXPORTER_OTLP_ENDPOINT is set (k8s deploy) — a
# no-op locally and in tests. The 1.27 / 0.48b line resolves cleanly against the
# fastapi 0.104 / starlette 0.27 pins above.
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp-proto-http==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0

# Environment variables
python-dotenv==1.0.0

# Logging and utilities
python-multipart==0.0.6
pydantic>=2.5,<3.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# TODO: Installation steps:
#   1. Create virtual environment: python -m venv venv
#   2. Activate: venv\Scripts\activate (Windows) or source venv/bin/activate (Mac/Linux)
#   3. Install: pip install -r requirements.txt
#   4. Then proceed to nutrition_analyzer.py
